<%= form_with(model: deck, local: true) do |form| %>
  <% if deck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

      <ul>
      <% deck.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :original %>
    <%= form.text_field :original, id: :deck_original %>
  </div>

  <div class="field">
    <%= form.label :target %>
    <%= form.text_field :target, id: :deck_target %>
  </div>

  <div class="field">
    <%= form.label :language %>
    <%= form.text_field :language, id: :deck_language %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
