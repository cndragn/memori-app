
<div class="imageresp">
  <%= image_tag "header2.jpg" %>
</div>

<br>
<br>
    <div class="row">

      <div class="col s12 m4 l4 center">
        <div class="card-panel yellow lighten-2">
          <%= link_to( ("<i class='material-icons'>bookmark_border</i>").html_safe, user_decks_path(current_user.id), { :class=>"btn-floating btn-large waves-effect waves-light black" }) %>
          <h3>MY DECKS</h3>
        <p>Check out the studydecks you already created.</p>
        </div>
      </div>

      <div class="col s12 m4 l4 center">
        <div class="card-panel yellow lighten-2">
            <%= link_to( ("<i class='material-icons'>add</i>").html_safe, new_user_deck_path(current_user.id), { :class=>"btn-floating btn-large waves-effect waves-light black" }) %>
          <h3>CREATE</h3>
        <p>Create your own studydeck with the words you want to learn</p>
        </div>
      </div>

      <div class="col s12 m4 l4 center">
        <div class="card-panel yellow lighten-2">
            <%= link_to( ("<i class='material-icons'>search</i>").html_safe, user_decks_path(current_user.id), { :class=>"btn-floating btn-large waves-effect waves-light black" }) %>
          <h3>FIND</h3>
        <p>Use our library of studydecks to find one you want to use</p>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col s12">
        <div class="card-panel yellow lighten-2">
          <h1>Popular decks</h1>
        </div>
      </div>
    </div>
<br>
<br>
